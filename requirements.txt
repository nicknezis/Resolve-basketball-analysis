ultralytics>=8.0
opencv-python>=4.8,<4.13
scenedetect[opencv]>=0.6
librosa>=0.10
soundfile>=0.12
numpy>=1.24
filterpy>=1.4
tqdm>=4.65
supervision>=0.20
trackers>=2.0

# Roboflow models (--detector roboflow / --roboflow-model / court keypoints). Runs locally via ONNX.
inference>=1.0
# SigLIP embeddings for team classification
transformers>=4.45

# Optional: RF-DETR detection backend
# rfdetr>=1.0

# Optional: pose estimation
# mediapipe>=0.10
